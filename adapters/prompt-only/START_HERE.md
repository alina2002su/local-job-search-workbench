# Prompt-only entry

Use this entry only when the current agent does not natively discover Agent Skills directories.

Please treat the attached `local-job-search-workbench` folder as a reusable Agent Skill.

1. Read `SKILL.md` completely and follow it as the authoritative workflow.
2. Resolve every referenced file relative to the folder containing `SKILL.md`.
3. Read only the supporting references needed for the current request.
4. First state whether you can read/write local files and run Python or shell commands.
5. If you have those capabilities, use full execution mode and verify observable results.
6. If you do not, use guided mode: produce the required choices, files, and exact user-run steps. Do not say anything was installed, started, or tested unless you actually verified it.
7. Never reuse another person's paths, credentials, identifiers, job data, resumes, branding, or browser data.

Current request:

> Help me build, configure, verify, or repair my own private local job-search workbench.
