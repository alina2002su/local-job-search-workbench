# 本地求职工作台 JD Clipper

该扩展仅在用户点击“保存 JD”后读取当前招聘页面，并把内容发送到本机工作台 API。保存目录由工作台配置返回，扩展不保存或猜测绝对路径。

## 加载

1. 启动本地工作台。
2. Chrome 打开 `chrome://extensions` 并开启开发者模式。
3. 选择“加载已解压的扩展程序”。
4. 选择本目录。

扩展只需要 `activeTab`、`scripting`、`storage` 和本机工作台地址权限；不读取 Cookie、浏览历史、表单输入、摄像头、麦克风或地理位置。
